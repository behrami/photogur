<!DOCTYPE html>
<html>
<head>
  <title>Photogur</title>
  <link href='https://fonts.googleapis.com/css?family=Quicksand' rel='stylesheet'>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class = 'navbar'>

    <div class= 'newPicture'><%= link_to(image_tag('add_photo_button.png'), "/pictures/new") %></div>

    <div class='title'><%= link_to "photogur", pictures_path %></div>

    <div class = 'right'>
      <% if current_user %>
        <div class = 'loggedin'>Signed in as <%= current_user.email %></div>
        <div class = 'logout'><%= link_to "Log out", sessions_path, :method => :delete %></div>
      <% else %>
        <div class = 'login'><%= link_to "Log in", new_sessions_path %></div>
        <div class= 'logout'><%= link_to "Sign up", new_users_path %></div>
      <% end %>
    </div>

  </div>

  <%= yield %>



</body>
</html>
